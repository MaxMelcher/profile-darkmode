import { FileiconDirective } from './fileicon.directive';

describe('FileiconDirective', () => {
  it('should create an instance', () => {
    const directive = new FileiconDirective();
    expect(directive).toBeTruthy();
  });
});
